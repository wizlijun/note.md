{
  "name": "mdrelay",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "wrangler": "^3",
    "@cloudflare/workers-types": "^4",
    "@cloudflare/vitest-pool-workers": "^0.5",
    "typescript": "^5",
    "vitest": "^2.1"
  },
  "pnpm": {
    "pnpm-note": "onlyBuiltDependencies mirrors root pnpm-workspace.yaml allowBuilds — required because pnpm 11 enforces per-package and workspace-wide build-script allowlists separately",
    "onlyBuiltDependencies": ["esbuild", "sharp", "workerd"]
  }
}
