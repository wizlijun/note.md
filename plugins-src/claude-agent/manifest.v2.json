{
  "manifest_version": 2,
  "id": "notemd.claude-agent",
  "name": "Claude Agent",
  "version": "1.0.16",
  "kind": "native",
  "engines": {
    "notemd": ">=6.827.2"
  },
  "description": "Run Claude Code headless against your vault: task templates live in .notemd/agent-tasks, stream live in a window, or fire detached from the CLI.",
  "binary": {
    "aarch64-apple-darwin": "bin/notemd-claude-agent",
    "x86_64-apple-darwin": "bin/notemd-claude-agent"
  },
  "ui": "ui/",
  "activation": {
    "events": [
      "onCommand:open",
      "onCommand:run",
      "onCommand:run-note",
      "onCommand:run-task",
      "onCommand:run-status",
      "onCommand:harness-status",
      "onCli:agent"
    ]
  },
  "contributes": {
    "menus": [
      {
        "location": "plugins",
        "label": "Claude Agent…",
        "command": "open"
      }
    ],
    "windows": [
      {
        "id": "main",
        "entry": "index.html",
        "title": "Claude Agent",
        "width": 900,
        "height": 640,
        "min_width": 640,
        "min_height": 420,
        "open_command": "open"
      }
    ],
    "settings": {
      "tab_label": "Claude Agent",
      "schema": [
        {
          "key": "notemd.claude-agent.maxConcurrency",
          "type": "select",
          "label": "Maximum concurrent AI reads",
          "options": ["1", "2", "3", "4", "5"],
          "default": "1"
        }
      ]
    },
    "cli": [
      {
        "subcommand": "agent",
        "command": "run",
        "summary": "Run a Claude Agent task headlessly",
        "args": [
          {
            "name": "task",
            "type": "string",
            "required": true,
            "help": "Task id — a directory under .notemd/agent-tasks/"
          }
        ],
        "flags": [
          {
            "long": "--prompt",
            "short": "-p",
            "type": "string",
            "help": "Extra prompt appended to the task's own"
          },
          {
            "long": "--wait",
            "type": "boolean",
            "help": "Run inline and wait for the result (max 300s) instead of detaching"
          }
        ]
      }
    ]
  },
  "i18n": {
    "zh": {
      "name": "Claude 智能体",
      "menus": {
        "open": "Claude 智能体…"
      },
      "settings.tab_label": "Claude 智能体",
      "settings.fields": {
        "notemd.claude-agent.maxConcurrency": "AI 阅读最大并行数"
      }
    },
    "ja": {
      "name": "Claude エージェント",
      "menus": {
        "open": "Claude エージェント…"
      },
      "settings.tab_label": "Claude エージェント",
      "settings.fields": {
        "notemd.claude-agent.maxConcurrency": "AI 読書の最大同時実行数"
      }
    },
    "de": {
      "name": "Claude Agent",
      "menus": {
        "open": "Claude Agent…"
      },
      "settings.tab_label": "Claude Agent",
      "settings.fields": {
        "notemd.claude-agent.maxConcurrency": "Maximale parallele KI-Lesevorgänge"
      }
    }
  },
  "capabilities": [
    "ui",
    "toast",
    "vault.read",
    "editor.open",
    "notify"
  ],
  "request_timeout_seconds": 300
}
