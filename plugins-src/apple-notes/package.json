{
  "name": "apple-notes-plugin",
  "version": "1.0.2",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vite": "^6",
    "vitest": "^4",
    "jsdom": "^25"
  }
}
