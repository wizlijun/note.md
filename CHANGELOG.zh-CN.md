# 更新日志

本文件记录每个发布版本里**用户能感知到的**变化。完整提交历史见 git log。

英文版:[CHANGELOG.md](CHANGELOG.md)

## 未发布

### 新增

- **文件索引支持四种阅读布局。** Index Viewer 插件将 `*.index.md` 显示为可搜索的多列表格、分组列表、泳道看板或封面网格。普通 Markdown 表格保存文件链接与字段，仅需少量可选布局设置；配套模板与 Agent skill 可基于真实文件生成索引。不支持的内容会回退到 Markdown 编辑器。

### 修复

- **Timeline 1.0.4 现在会准确定位到配置的打开时间。** 时间轴改为在自身滚动区域内计算日程位置，标题、工具栏和图例不再造成约两小时的可视时间偏移。

## v6.909.6 — 2026-09-09

### 新增

- **标签页右键菜单现可批量关闭文件。** 右键标签后可关闭当前文件、关闭左侧标签或关闭所有标签；修改过的文件仍沿用现有保存与放弃确认，取消后会停止剩余批量操作。

### 修复

- **插件变更现在无需重启 note.md 即可生效。** 安装、更新、重新安装、启用、停用或移除插件后，运行时、菜单、快捷键和已打开的插件视图会立即刷新；插件市场也会提示被关闭的视图和重载失败。

## v6.909.5 — 2026-09-09

### 修复

- **Note 视图图标现在与相邻模式按钮保持一致。** 光芒图形保持不变，颜色和选中状态改为与 Rich、Source 及其他文件视图统一。

## v6.909.4 — 2026-09-09

### 新增

- **Note 文件改用统一的格式视图切换。** `.note.md` 和 `.notes.md` 的内置大纲现与插件视图一样，通过 Rich、Source 旁的统一入口显示；Note 保留橙色光芒图标，Timeline 改为圆形线条时钟。
- **Timeline 1.0.3 可设置打开时的定位时间。** 时间线设置新增定位时间，默认 09:00，并将分类和时间作为一份配置保存。切换上一天、下一天或指定日期时，会直接替换当前页签里的文件，不再新增页签。

## v6.909.3 — 2026-09-09

### 新增

- **文件视图已融入 Rich/Source 切换。** 符合规则的插件会在宿主紧凑分段控件中获得一个视图入口；载入或解析失败回到内置编辑器后，也可从同一位置重试。宿主为这组控件预留界面空间，不再覆盖插件内容；插件导航还可在打开匹配文件时继续选中自己的声明视图。
- **Timeline 1.0.2 新增紧凑日期导航。** 可前后严格移动一天，或点击日期选择任意一天；平铺 diary 与按年份归档都能正确跨年。目标日期不存在时保留当前时间线，且不会创建文件。

## v6.909.2 — 2026-09-09

### 修复

- **时间轴现在可以正常完成载入。** 宿主将插件桥接改为符合 CSP 的同源脚本，文件视图可正确确认文档，不再停留在“正在载入文件视图…”。Timeline 1.0.1 同时新增「插件 → 回顾 → 查看时间轴」；有匹配文件时查看当前文件，没有打开文档时先显示文件选择器。请先将 note.md 更新到 6.909.2，再安装插件更新。

## v6.909.1 — 2026-09-09

### 新增

- **按文件规则自动使用插件视图。** 插件可声明扩展名、文件名、路径和 frontmatter 属性条件，由 note.md 统一选择显示方式；源码编辑始终可用，插件加载或解析失败时恢复该文件原有的内置编辑器。
- **用竖向日程查看每日时间线。** Timeline 1.0.0 支持交叠活动、短时刻和嵌套议题，用五类浅色区分活动。可在界面修改关键词规则，分类只匹配活动类别中包含的关键词。请先更新 note.md，再从插件市场安装“时间轴”并重新启动。

## v6.908.1 — 2026-09-08

### 变更

- **新建后直接开始写，无需先取名。** 新建文档、快捷键与托盘 Quick Note 统一在 Vault 配置的 inbox 目录创建空白临时笔记；新建画布直接在 `Vault/canvas` 创建 `untitled.canvas`，重名自动追加序号，不再弹出保存对话框。
- **保存时自动生成正式文件名。** 画布根据第一张文本卡片的标题生成 `YYYY-MM-DD-slug.canvas`；没有有效标题时使用 `YYYY-MM-DD-HHmmss.canvas`。新文档采用相同的日期与标题规则，以 `.md` 结尾。已命名文件保持名称，重名自动追加序号；画布自动保存保留临时名称，手动保存时再正式命名。

### 修复

- **画布导航与多选操作保持一致。** 平移、按住 Space 平移和交互锁定不再误拖卡片；从多选区域的空隙拖动后，位置可正确保存，并通过一次撤销完整恢复分组及成员。画布输入框和弹层支持键盘焦点，上下文操作栏保持在画布边界内。
- **保存期间继续输入不会丢失未保存状态。** 文件写入与自动命名统一协调，防止自动保存重新创建旧临时文件名；保存过程中继续编辑或撤回到较早内容时，仍正确显示未保存。

## v6.906.2 — 2026-09-06

### 新增

- **一条命令增量同步会议。** 会议插件 1.0.3 新增 `notemd meetings-sync`，将 Hemory 新增或变化的逐字稿归档到当前 Vault 的会议目录。支持指定来源目录、`--user`、`--timezone`、`--dry-run` 和 JSON 报告；重复执行跳过未变会议，遇到本地编辑时保留内容并报告冲突。请先更新 note.md 到 6.906.2，再从插件市场更新会议插件并重新启动。

### 修复

- **插件 CLI 可正确接收来源目录。** 不需要文档上下文的命令不再把路径参数当作文本文件读取，修复指定 Hemory 来源目录时的执行失败，同时保持 PDF 命令的文件读取与渲染；会议同步命令结束后也会正确退出插件后台进程。

## v6.906.1 — 2026-09-06

### 变更

- **16 个正式插件配套发布。** OpenClaw 首次进入插件市场，开发测试插件不包含在内。请先更新 note.md，再更新插件并重新启动。Memory 2.5.2 的完整文档编辑器需要 note.md 6.906.1 或更新版本；升级前请备份共写文档，若需回退，应同时恢复匹配的数据与软件版本。
- **Memory 升级提醒：** 下一次成功保存会将共写文档状态升级为 v6。升级前请保存或导出未保存草稿，同时备份 Markdown 正文和应用数据目录中的 `plugin_data/notemd.memory/`。旧插件不能读取 v6，仅降级插件不能完成回退。
- **设置与插件窗口更清晰，也更适合键盘操作。** 窄窗口下保留设置导航和底部操作；插件统一辅助文字、浅深色语义、可见焦点和弹层菜单，同时保留各自的工作流程。设置与确认弹窗保护正在进行的操作，保存失败时提供明确反馈；Agent 聊天重连时保留未发送草稿。

### 修复

- **Memory 共写文档支持整篇编辑。** 跨块选区、剪切粘贴、全文清空、文末空段与硬换行可正确保存，保存期间继续输入或使用输入法不会再被打断。补齐格式、列表、链接、图片与表格入口，以及未保存草稿恢复、比较和重试；撤销会核对目标内容，并支持外部移动后按块身份恢复文字。更新后的 Memory 插件与宿主 Editor Kit 需配套部署。

## v6.905.6 — 2026-09-05

### 变更

- **智能查找直接呈现结果。** 匹配笔记与首条原文预览会直接显示，仅保留一个“打开原文”入口，复制引用、快速简答和深入研究统一收进“更多”菜单。窗口不再突然改变尺寸，支持键盘浏览和窄窗口布局，并通过可滚动工作记录与明确的“停止”操作反馈进度。
- **涉及时间的问题先限缩范围，再检索。** 快速理解阶段基于冻结的本地日期和时区，把文档时间约束与搜索主题分离；校验后的时间边界应用于每条检索，不再将“上周”等表达直接当关键词。请同步更新 Claude Agent 1.0.30、Codex Agent 1.0.17 或 DeepSeek Agent 1.1.16，使用对应的时间解析指令。

### 修复

- **中断的任务不会再覆盖新的结果或简答。** 停止、修改问题或切换笔记选择后，迟到响应会失效。失败时保留已有匹配，显示简短可操作的提示，不暴露服务原始错误；复制研究说明也不会误启动 Agent。

## v6.905.5 — 2026-09-05

### 变更

- **目录当前阅读位置的高亮更加简洁。** 保留醒目的蓝色高亮，同时去掉左侧多余的深色强调条。

## v6.905.4 — 2026-09-05

### 变更

- **目录视图现在会跟随文章的当前阅读位置。** 目录打开时，在富文本或源码模式中滚动 Markdown 文章，都会醒目标出当前对应标题；标题重名时仍能准确区分。

## v6.905.3 — 2026-09-05

### 新增

- **新增只读目录视图，长篇 Markdown 文章可更快浏览与跳转。** 在手记和历史旁打开新的大纲式目录，即可预览当前文档的标题层级（包括尚未保存的编辑），并直接跳到任意章节；标题重名时也能准确定位。

## v6.905.2 — 2026-09-05

### 变更

- **无限画布的操作入口更集中，也更贴近当前内容。** 底部居中的图标工具坞汇集了新建、导航、剪贴板和撤销/重做操作；选中节点、多选、分组或连线时，会在画布内容附近显示对应操作。卡片、分组标签、连接点及悬停/选中状态也获得更清晰的视觉反馈，并支持一次为多个选中节点设置同一颜色。

## v6.905.1 — 2026-09-05

### 新增

- **无限画布补齐了更完整的编辑工具。** 可在选择、平移、自由套索和框组选区之间切换，通过智能参考线对齐、分布或散开重叠内容，对多个卡片整体缩放，并让分组自动适配内部内容。连线会选择合理的相向端点，标签可原位编辑；触控绘制遇到第二根手指时会交还导航，新建和粘贴卡片会落在最后指针位置，视图锁定、低缩放简化和索引化几何计算也让大画布更易浏览。

## v6.904.8 — 2026-09-05

### 修复

- **受控 Memory 文档现已支持正常的 Markdown 基础编辑。** 在同一个稳定知识块内可使用 Enter、Shift+Enter、多段粘贴、文字插入与删除以及撤销重做；连续输入会合并后再可靠保存，中文输入法内容和已确认的编辑器状态不会丢失或被重建。跨越受控块边界的编辑仍会安全停止，不会猜测块身份。需配合 Memory 2.5.1。

## v6.904.7 — 2026-09-04

### 新增

- **Memory-first 受控共写预览。** Memory 2.5.0 可在独立窗口创建并局部维护一份带稳定块身份的背景文档；正文、提案、精确版本检查与审计通过同一持久化提交恢复。Agent 建议默认等待人工接受，旧版本结果不会覆盖并发编辑；配套 Claude Agent 1.0.29、Codex Agent 1.0.16 与 DeepSeek Agent 1.1.15 在真正的 input-only 隔离中运行通用文档检查任务。

### 安全

- **受控共写不会信任 Vault 内的 Agent 任务文件作为隔离边界。** 三家 provider 编译内置同一任务协议，Memory 仅允许明确声明精确任务、完整机器结果和 input-only 隔离能力的健康 provider；旧 provider 会直接禁用该入口。

## v6.904.6 — 2026-09-04

### 修复

- **无限画布上的可见控件现在都会执行对应操作。** 分组可以围绕选区创建，并带着所有完全包含的卡片一起移动；嵌套组和重叠组选区不会重复位移，同时支持调整尺寸、重命名、背景样式及保留内部卡片的解组。连线重连、标签、两端箭头和颜色会正确写回 `.canvas`；撤销重做会即时更新，键盘移动可持久保存，桌面与 iOS 的剪贴板回退和触控命中区也更加可靠。

## v6.904.5 — 2026-09-04

### 新增

- **无限画布带来兼容 Obsidian 的可视化工作区。** 可创建和打开标准 `.canvas` 文件，排列文本、文件/图片、链接、分组卡片，添加带标签的连线，并使用平移缩放、多选、调整尺寸、复制粘贴和撤销重做。画布通过受保护的原子保存、自动保存和外部变更处理保证文件可靠性，往返时保留未知 JSON Canvas 字段；Markdown 卡片使用安全预览且同一时间只挂载一个活动编辑器，Svelte Flow 也只在打开画布标签页时加载。桌面文件拖入与 iOS Open In/导出副本均经过有界本地资源访问，不嵌入远程网页。

## v6.904.4 — 2026-09-04

### 变更

- **YAML Frontmatter 不再像表格，而是更清晰的元数据卡片。** 富文本编辑器与分享页面统一使用轻量圆角属性面板、紧凑键值行和标签式标量列表。Wikilink、Markdown 链接与裸网址在阅读时可识别、可点击，进入编辑后仍保留完整源文。

- **智能查找会先缩小时间敏感问题的范围，再执行检索。** 单次快速 Planner 会先为当前状态、近期进展或最近一轮工作估算保守的文档日期窗口，再在范围内选择检索词。用户明确给出的 `after`/`before` 始终优先；常青知识或确有歧义的问题仍保持全范围，求交后的实际范围也会一致传给后续 Agent。Claude Agent 1.0.28、Codex Agent 1.0.15 与 DeepSeek Agent 1.1.14 包含配套规划契约。

## v6.904.3 — 2026-09-04

### 修复

- **所有者资料重新投影到 `USER.md`。** 身份、偏好、工作方式与边界进入 `user` 投影；决定、承诺、实践和其他长期背景继续留在 `MEMORY.md`。两份文件仍是按 Scope、Role 分组的可丢弃只读视图，唯一权威仍是 `.notemd/memory`。

## v6.904.2 — 2026-09-04

### 修复

- **智能查找不再在快速 Planner 正常返回首个结果前将其误取消。** 默认理解等待时间按实际 harness 启动耗时调整，仍可设置且保留硬上限；既有的 8 秒默认值会自动迁移。Codex 后台模型目录刷新较慢时，明确指定的快速模型仍保持可用，普通本地匹配会继续显示，provider 内部诊断也不会再取代真正的超时或取消状态。

## v6.904.1 — 2026-09-04

### 新增

- **Memory 2.4.3 允许外部 Agent 安装已校验的 Role/Scope Registry，无需再由用户逐项录入。** `notemd memory context-registry replace --file ... --request-id ...` 会把完整 JSON 或 YAML 候选交给与 Memory UI 相同的 exact-head、哈希链 Host 事务并立即生效；稳定 request-id 可安全重试，而 Claim 审批与重新分配 apply 仍仅限可信 UI。

## v6.903.6 — 2026-09-03

### 变更

- **“智能搜索与问答”现已收敛为“智能查找”：把自然语言需求快速变成有用的 Vault 结果。** 输入时仍提供零 Token 预览；按回车只调用一次快速 Agent 规划，将时间、路径、标签、来源类型和排序与真正的检索词分离，再由可信宿主执行 typed search 并展示可解释的结果卡。主链不再自动调优、加载 Memory、生成回答、归档反馈或写详细文档。

- **深度工作改为用户明确交给完整 Agent。** “快速简答”只会在硬预算内总结少量完整的行级命中，并明确标注“基于当前匹配”；“交给 Agent 深入研究”只传问题、结构化约束与 Vault 相对引用，由 Claude、Codex 或 DeepSeek 通过 `notemd` 重新检索，并在自己的策略下申请 Memory 上下文，不复制整库内容。

- **智能查找的体验参数可以设置，但安全上限不会被放宽。** 可配置规划 Agent、等待时间、结果/分组数量、零结果时是否深搜、简答预算/风格和默认交接 Agent。Claude Agent 1.0.27、Codex Agent 1.0.13 与 DeepSeek Agent 1.1.13 新增统一的 input-only `search-summary`、只读 `vault-research` 交接、幂等 invocation 恢复和取消能力。

### 修复

- **长文档或长章节不会再让新流程以 `invalid source line range` 中止。** 文件级和章节级命中只作为诚实的导航结果，不会被截断后冒充回答证据；快速简答只接受完整、当前有效的行级块，并跳过过期或超预算来源。已退出主链的旧回答命令再保留一个版本的有界兼容保护，非法路径、倒序范围和内容变化仍会严格拒绝。

## v6.903.5 — 2026-09-03

### 变更

- **智能搜索在理解问题和生成回答时，会持续显示可滚动的实时工作记录。** 界面会安全展示规划、检索、调优、证据、记忆与回答等当前阶段，不泄露 provider 内部指令；无论成功或失败，记录都会保留。若回答生成失败，可直接只重试回答并复用已经冻结的证据，不会重复检索。

### 修复

- **智能搜索现在更能承受 Agent 响应较慢或状态读取短暂失败。** 规划阶段获得更长的静默等待窗口，只读状态轮询会原地重试，失败时也会快速显示所在阶段，不再只留下笼统的超时。Claude Agent 1.0.26、Codex Agent 1.0.12 与 DeepSeek Agent 1.1.12 包含配套任务更新；Codex 还改用当前 CLI 的功能禁用参数，并保留进程早退时的 stderr 诊断。

## v6.903.4 — 2026-09-03

### 变更

- **智能搜索现在会先理解问题，再进行检索。** 所选 Agent 会把自然语言意图转换为经过严格校验的搜索计划；“上个月”等日期会成为按时区解算的文档约束，不再被当成关键词，同时路径、标签、类型、来源和排序仍由宿主锁定。召回不足时最多执行一次受控调优，随后宿主重新读取并冻结实际引用的 Vault 行，再交给 input-only Answer 运行。输入时依旧显示零 Token 的“快速预览”，并与权威“智能结果”明确区分。

- **Claude Agent 1.0.25、Codex Agent 1.0.11 与 DeepSeek Agent 1.1.11 新增分阶段模型路由。** Plan/Tune 默认使用各自 Harness 的快速模型，Answer 保留独立的默认/质量模型；智能搜索的模型菜单会按 provider 与阶段分别保存选择。每次运行都会记录实际解析模型，三家也统一提供 input-only `search-plan` 任务及安全编排所需的能力协商。

- **Memory 2.4.2 可以复制一份供外部 Agent 使用的严谨 Role/Scope 初始化 Prompt。** 分阶段流程只根据可归属的 Vault 证据生成候选，将不同客户与家庭放进独立安全域，校验完整 Registry 候选，并在所有者确认后才准备精确的 Claim 重分配提案；Prompt 明确禁止不存在的 Registry 写命令、直接 apply、伪造人类身份、输出凭据以及对跨 Realm 歧义强行分类。

- **Memory 2.4.1 将身份与场景治理融入主工作区。** “身份与场景”现在与已确认记忆、待确认建议、冲突与历史并列为一级页签，不再通过独立弹层打开；使用中的 Role/Scope 数量始终可见，Registry 编辑与原子批量重分配能力保持不变，也不会打断其余审阅流程。

## v6.903.3 — 2026-09-03

### 新增

- **Memory 2.4.0 让同一个人的多重身份与工作场景真正隔离。** 你可以在 Memory 窗口统一创建、改名、归档和恢复 Role/Scope，先预览再批量重新分配既有记忆；Agent 也可通过只读/提案式 CLI 做批处理，但不能绕过人工批准。所有内容仍只投影到一个 `MEMORY.md`，按 Scope → Role 分组，并带明确的 Agent 使用边界；旧 `USER.md` 投影会在重建时移除。批量应用绑定当前 Registry、主张 heads 与预览哈希，中断或过期计划不会产生半生效结果。

## v6.903.2 — 2026-09-03

### 新增

- **智能搜索与问答把 Vault 变成一个键盘优先的研究入口。** 在应用内按 Command/Ctrl-K，或在 macOS 任意位置按 Option-Space 即可唤起；输入时结果即时出现并按真实来源分组，可直接在编辑器中打开，也可单条或标准多选后从本次回答移除，原文件不受影响。按回车会让选中的 Claude、Codex 或 DeepSeek Agent 基于冻结的证据包和策略允许的 USER、MEMORY 事实作答；回答保留可点击引用，点赞会把可审计的 Markdown 归档到 `answers/`，另一个明确动作可生成更详细、可继续编辑的笔记。

- **会议 1.0.0 可把 Hemory 对话安全迁入 `ssot/meetings/`。** 插件窗口与 `meetings-import-hemory` CLI 共用同一套先 dry-run 的迁移引擎，保留原始带时间和说话人的正文及可选摘要，只写入已确认的扁平元数据，绝不复制音频，并用哈希、checkpoint 与恢复 journal 保护本地修改、重复导入和中断事务。

- **你亲手写的东西,现在文件里就写着是你写的。** 新建文档、快速笔记、手记 `.note.md`、日期笔记、wikilink 建页、奇思妙想的 idea 原文、溯源的委托稿 —— 这些由你一个动作直接产生的文件,元信息里多了一行署名(`generated: by: human:<你的 git 身份>`)。此前「是人写的」只能靠「没有 AI 署名」反推,一份忘了盖章的 AI 产物和你的手稿在元数据上分不开;现在三种状态各说各话:人写的、机器写的、没人认领的。署名只在**创建**时写一次,打开旧笔记再保存不会凭空长出署名;从别处导入的内容(Roam、电子书)也不签 —— 搬运不是撰写。换到 Obsidian 或 grep 里一样读得出来。

### 变更

- **Claude Agent 1.0.24、Codex Agent 1.0.10 与 DeepSeek Agent 1.1.10 新增共享的 input-only `search-answer` 任务。** 任务只接收宿主冻结的问题、获准记忆事实和搜索片段；运行目录在 Vault 外，不继承当前标签页或源文上下文，拒绝工具权限升级，并让短回答与详细 Markdown 共用同一套引用和证据契约。

- **Claude Agent 1.0.23、Codex Agent 1.0.9 与 DeepSeek Agent 1.1.9 的最近运行现在每次展开 5 条。** 窗口初始只显示最新 5 条，并提供紧凑的“再显示 5 条”操作；每次点击继续展开 5 条，最后一批会按实际剩余数量显示。切换任务或历史范围时回到最新 5 条，后台刷新则保留已经展开的数量。

- **Claude Agent 1.0.22、Codex Agent 1.0.8 与 DeepSeek Agent 1.1.8 现在优先展示最近运行，并把任务模板保持紧凑。** 任务按实际使用它的功能插件分组，所有分组默认折叠，让侧栏把主要空间留给最新运行历史；展开中性的插件行即可选择任务，折叠时仍能看到任务数量与运行中状态。旧 Vault 无需迁移即可识别，自定义任务也保留在独立分组中。

### 修复

- **Command/Ctrl-A 现在再次能在主编辑器的每种模式中选中全文。** 源码模式不再把快捷键交给原生菜单响应器，富文本与源码编辑也继续使用一致的选区处理。

- **富文本里点一个还不存在的 `[[链接]]`,不再建出一个空壳文件。** 它此前写的是 0 字节文件:没有标题、没有元信息,格式校验会报错,检索里也排在最末。现在和大纲面板走同一条建页路径,建出来就是一份合规文档。

## v6.903.1 — 2026-09-03

### 新增

- **Memory 待确认建议现在支持多选与安全的批量清理。** 普通点击单选，Command/Ctrl 增减选择，Shift 连选；右键选区即可批量忽略、否认或删除候选，并先展示准确数量和影响。人工确认、编辑与“认为重要”仍逐条进行，避免把身份表达、事实确认、行为授权和生命周期变更混在一次含糊操作中批准。

### 变更

- **Claude Agent 1.0.21、Codex Agent 1.0.7 与 DeepSeek Agent 1.1.7 现在使用更清晰的统一工作台。** 运行环境、任务和历史共用一个可访问的侧栏滚动区，设置则固定在底部，较长的任务列表不再把历史挤出可视范围；双行历史卡片保持状态、任务、时间和 Agent 来源清晰可读，输入、运行流、日志和设置表面也与插件市场的中性视觉语言统一，并补齐 hover、选中和键盘焦点反馈。

- **`notemd` CLI 现在对内建命令和插件命令提供完整、严格且一致的契约。** 顶层与主题 help 会准确列出真实参数、值占位符、JSON envelope、quiet 行为与退出码；Memory 也按文档提供精确主张查询、过滤列表、上下文 manifest、清理计划和感知漂移的检查。

### 修复

- **并发变化不再让待确认批量操作看起来像虚假的“全有或全无”。** 每项都带精确校验并串行处理；某项过期时会立即停止、刷新真实队列，并明确报告已完成与剩余数量。

- **插件与自动化命令不再静默曲解错误输入。** 动态插件参数会保留 manifest 声明的名称与类型，已禁用插件可查看并重新启用，未知或冲突 flag 会安全失败。Share 在源文件已删除后仍可清理分享记录，Reading Insights 会一致校验日期范围，纯 CLI 运行不再激活无关启动插件，WebView 命令也会超时退出而不是永久卡住。

## v6.902.9 — 2026-09-02

### 新增

- **现在可以把别的 AI 助手记住你的内容收进 Memory。** 记忆窗口里的「复制导入记忆Prompt」会复制一段现成的 Prompt；粘贴给 ChatGPT、Gemini 或任何带记忆功能的助手，它就会把自己记住的条目导出成 `notemd memory propose` 命令。在 vault 里运行这些命令，全部以待确认主张入库 —— 不经你在这里确认，任何一条都不会进入记忆。

- **已确认记忆现在会按投影位置与二级分类分组。** `MEMORY.md`、`USER.md` 与仅结构化上下文各自成组，并显示分类数量；默认在各组内重点优先，其余顺序与纯文本投影一致，也可切换为最近更新、最早更新或正文排序。

### 变更

- **决策 1.1.6 全面统一为更简洁的产品名。** 插件菜单、窗口、看板、市场以及四种语言名称现在统一显示为 Decision / 决策 / 意思決定 / Entscheidung；插件 ID 与已有数据保持不变。

### 变更

- **决策 1.1.6 全面统一为更简洁的产品名。** 插件菜单、窗口、看板、市场以及四种语言名称现在统一显示为 Decision / 决策 / 意思決定 / Entscheidung；插件 ID 与已有数据保持不变。

### 修复

- **提案主张现在可以允许多个检索用途。** `notemd memory propose` 的 `--purpose` 支持逗号分隔，与记忆窗口里的多选一致；此前命令行提出的主张只能有一个用途，在其他场景下会被一律扣留。`replace` 与 `revoke` 也不再强制要求它们随后会忽略的 `--category`、`--space` 和 `--purpose`。

## v6.902.8 — 2026-09-02

### 新增

- **Memory 2.2.1 允许人工修正已确认与待确认记忆的文字。** 编辑已确认主张会生成可审计的 replacement，而不是改写历史；编辑待确认建议则会在一次操作中批准修正文案，同时保留原始提案。精确 revision heads 会阻止覆盖并发变更，生命周期提案也不会被正文编辑改变语义。

## v6.902.7 — 2026-09-02

### 修复

- **首次合法的 Memory 提案现在会自动初始化 Memory Protocol v2。** 外部 Agent harness 可直接向完全空白的 Vault 写入待确认 `propose create`，无需先通过受信任 UI 单独初始化；只读或非法命令、不完整仓库、restricted 内容和已有非空投影仍会失败关闭，并发首次写入则共用同一次幂等协议初始化。

## v6.902.6 — 2026-09-02

### 新增

- **Claude Agent 1.0.20、Codex Agent 1.0.6 与 DeepSeek Agent 1.1.6 会在每次运行结束后报告 Token 用量与费用。** 每个插件都可选择在完成提示或结果区显示摘要，并始终保存在运行历史中；供应商实报费用优先，否则仅对可确定模型使用带日期的官网 API 标价估算，并明确标为估算而非订阅消耗或实际账单。Claude 区分 prompt cache 写入时长，Codex 不会用整轮聚合量猜测逐请求长上下文加价，DeepSeek 则按完成时刻选择 UTC 峰价或谷价；若执行框架没有上报用量，仍明确显示未报告，不会按文本长度推算。

## v6.902.5 — 2026-09-02

### 新增

- **Memory 2.2 可通过一次受保护操作清空并重构全部当前记忆。** 写入前，插件会准确展示将从 `USER.md`、`MEMORY.md` 与 Agent 上下文移除的已确认主张和待确认建议数量；确认后会移除这些当前视图、安全收口并发 heads、将下次推理重置为全量 Vault 扫描，同时保留所有者身份、Memory 协议与不可变审计历史。

### 变更

- **Memory 推理现在宁可不给建议，也不提交弱建议。** 六项质量门只接受 Vault 所有者明确说过、长期且有未来用途的内容，排除任务、实现细节、产品默认值、文档摘要和单次行为推断；已确认、待确认、已否认与已忽略历史全部参与语义去重，正常运行通常只提出零到五条，不再把上限当目标。

### 修复

- **溯源 1.2.3 会直接在自己的窗口里打开并编辑收件箱项目。** 已完成报告和已保存委托稿都会载入插件现有编辑区，不再唤起主编辑器；修改自动写回原文件，切换、慢写入、删除与关窗按顺序收口，避免丢失最新文字或在删除后重新创建文件。

## v6.902.4 — 2026-09-02

### 变更

- **Ebook Import 1.3.5 将可管理主题上限从五个放宽到八个。** AI 主题设计会按自然领域边界在 2–8 个之间选择，不要求凑满上限，也不会只为压缩数量而合并不相关领域；人工主题管理与批量分类同步接受扩展后的目录。

### 修复

- **插件 Editor Kit 窗口在 rich 与 source 模式下都能用 Cmd+A 或 Ctrl+A 选中全文。** rich 编辑会在代码块缩小选区前接管快捷键，source 编辑则显式选中 textarea，不再依赖内嵌 WebView 的原生 responder。

## v6.902.3 — 2026-09-02

### 新增

- **Ebook Import 1.3.4 可将全部未分类旧书交给 AI 批量建议并集中确认。** Agent 会有界读取最新 AI 摘要、章节标题、正文开头和书籍元数据，只能选择已有主题；每本建议都可在写入前人工修正，最终一次事务确认更新书籍元数据并重建全部生成主题索引。

### 修复

- **Memory 2.1.1 可在 Codex workspace 沙箱中正常持久化 AI 推理建议。** 仓库写锁不再写入受保护的 Git metadata，linked worktree 仍共用同一把锁；只有存在与本次调用匹配的推理水位时才会报告成功。扫描期间产生的新提交会安全留到下一次增量补扫，不再使当前运行失效。
- **Claude Agent 1.0.19、Codex Agent 1.0.5 与 DeepSeek Agent 1.1.5 为应用工作流保留完整终态响应。** 面向人的运行历史仍只保存 8 KiB 摘要，独立且有界的完整结果通道避免长 YAML 方案从截断尾部开始解析；Codex 还会合并多段最终消息，Ebook Import 在协议响应不合法时会自动纠错重试一次。

## v6.902.2 — 2026-09-02

### 变更

- **插件市场现在会在副标题下显示当前 note.md 版本和重启提示。** 安装或更新插件后，界面会提醒用户退出并重新启动 note.md，让启动期装载的集成能力稳定生效。

### 修复

- **Ebook Import 1.3.3 恢复了 AI 主题设计的 Agent 选择器。** 主题设计现在可独立记忆 Claude、Codex 或 DeepSeek，不再与「AI 先读」共用选择；界面会明确提示所选 Agent 是仅能读取 inventory，还是在只读任务策略下可能读取整个 Vault。
- **插件市场的刷新按钮现在会获取最新线上目录，不再接受缓存索引。** 用户主动刷新会使用唯一请求地址和明确的禁止缓存策略；启动时仍会优先从本地缓存显示已安装插件。
- **主程序原生 Plugins 菜单现在与插件市场的当前分类布局一致。** AI 分类置顶；Memory 与 Claude、Codex、DeepSeek Agent 归入 AI，Next 与 OpenClaw Chat 保持在「推进」。

## v6.902.1 — 2026-09-02

### 新增

- **Memory 2.1 可以让选定的 AI Agent 从现有 Vault 推理长期 owner 记忆。** 第一次成功运行执行全量扫描，之后以成功 Git 水位执行增量推理；所有结果仍是等待人工审阅的 pending Claim，任务协议内置 prompt injection、secret、第三方信息与 revoke 安全边界。

### 变更

- **Memory 现在只有纯 v2 路径。** Host、CLI 与插件不再暴露 v1 兼容、迁移 RPC 或旧投影 marker；空 Vault 会由受信任的 UI 操作直接初始化为 Memory Protocol v2。

### 修复

- **macOS 安装镜像现在会携带外层公证票据。** Apple Silicon 与 Intel DMG 都会在发布前单独提交 Apple 公证、装订票据并通过 Gatekeeper，不再只依赖镜像内部已经公证的 App。

## v6.901.10 — 2026-09-02

### 修复

- **Ebook Import 1.3.2 不再因一个遗留目录而中断整次 AI 主题设计。** 目录名尾随空白等不安全书籍路径会从 AI inventory 中跳过，其余合法书籍继续完成分类；手工构造的不安全 inventory 仍会被协议层严格拒绝。
- **Memory 2.0.1 现在会在批准、authority 与并发 Git 路径上严格失败关闭。** 控制面修订必须证明因果权限；并发的批准/否认决定会保持显式冲突；request 幂等性在单 clone 中受锁保护，并可跨 clone 确定性收敛；Context Manifest 绑定精确预览；生成的 `USER.md` 和 `MEMORY.md` 会被识别为只读投影，行动敏感冲突期间保留保守警示。
- **Ebook Import 1.3.1 将主题整理改为事务化且受能力边界约束的流程。** 主题目录使用 revision 检查和可回滚的索引重建，拒绝符号链接越界，中断恢复会校验当前书库，并发 AI 状态不再丢失；不可信 metadata 在处理前，主题设计只接受能验证单文件读取白名单的 provider。
- **插件更新不再将旧窗口与新后端拼接运行。** 被替换的 runtime 会被永久隔离，旧插件窗口会在已安装包切换前销毁；只有完整 manifest 与安装目录均一致时，才会复用同 ID 进程。

## v6.901.9 — 2026-09-01

### 新增

- **Memory 2.0 引入由 Git 支撑的个人 Claim 台账。** 不可变 YAML 修订现在按主体、断言、批准含义、有效时间、信任层、风险和场景范围区分偏好、边界、决定、信念、观察与物质事实。Agent 只能为 Vault owner 提出 pending 主张；人工决定会绑定精确的 protocol、authority 与 revision。
- **导入电子书现在会把每本新书归入一个受管理主题。** 书库可以定义最多五个主题，在导入时选择其一并自动维护主题索引；也可以让已配置的 AI provider 根据现有藏书设计主题体系，同时不授予不受控的文件写入权限。

### 变更

- **`USER.md` 与 `MEMORY.md` 现在是可丢弃的纯文本投影。** Host 从 `.notemd/memory` 确定性重建一层分类和多行事实，不再把过程元数据写入 Markdown；authority 损坏或源记录冲突时会失败关闭。
- **Memory 在多设备和外部模型场景中保持保守。** 不同 Git clone 的不可变记录按集合合并，同一 Claim 的并发修改必须显式解决；行动敏感冲突会收紧权限，Context Manifest 则记录 Agent 上下文使用的 Space、目的、调用方、provider、model、工具与选择原因。
- **AI 现在是插件市场最顶部的系统分类。** Claude Agent、Codex Agent、DeepSeek Agent 与 Memory 统一归入该分类；即使已安装插件元数据或缓存目录仍保留旧分类，也会自动迁移显示。

## v6.901.8 — 2026-09-01

### 变更

- **插件市场现在采用更克制的标准窗口配色。** 窗口、分区、卡片、更新与 AI 区域统一使用中性系统色；分类和插件图标保留识别色，操作控件统一使用系统强调色。
- **手工确认 Memory 现在只需一次操作。** 点击事实的「确认」或已审阅候选的「批准」会直接写入绑定 SHA 的人类决定，不再要求第二次确认。
- **Memory 投影不再在每条事实后显性展示引用备注。** 行内脚注标记、脚注定义与逐条来源属性不再写入 `USER.md` 和 `MEMORY.md`；不可变候选保留来源，决定事件绑定候选的精确 SHA。

## v6.901.7 — 2026-09-01

### 新增

- **Memory 事实现在支持一键审阅。** 无需填写完整元数据表单，即可确认、否认、标为重要、设为可忽略或删除事实。待确认事实会复用精确的原候选，所有写入仍须通过窗口内绑定 SHA 的确认步骤。

### 变更

- **删除记忆现在具有明确、可审计的语义。** 批准后只从当前 `USER.md` 或 `MEMORY.md` 投影中移除对应条目，不可变候选与决定事件仍保留；人工编写但缺少精确目标修订的更新候选会失败关闭。
- **Next 卡片标题现在就是直接编辑入口。** 点击 Idea 或 Task 标题即可打开内容与元数据编辑器，不再在卡片上增加一个竞争性控件。

## v6.901.6 — 2026-09-01

### 新增

- **Memory 条目现在明确区分审阅状态、含义与证据。** 每条记录可分别标记 critical/high/normal/low 优先级、正向/负向/中性行为方向、证据性质、确定度、明确的 Agent 使用规则与「必须避免」规则。旧条目会保守迁移为中性和未知，不会被自动晋升成确定事实。

### 变更

- **Memory 采用专注的 macOS 风格主从审阅窗口。** pending、负向与 critical 内容更容易辨认，字体和控件保持一致；写入决定前，窗口内确认 sheet 会完整展示候选身份、SHA-256、正文及行为元数据的精确变化。
- **Next 现在可以完整编辑 Idea 与 Task 卡片元数据。** 点击 project、priority、due 或 context 标签即可打开完整编辑器，并保持 Markdown frontmatter 与生命周期台账一致。

### 修复

- **插件窗口激活后的第一次点击现在即可命中控件。** Memory 不再依赖浏览器确认框，窗口被唤起后所有交互控件都能立即响应。
- **无效 Memory 更新会失败关闭。** 更新候选必须包含非空目标，owner 身份只走专用 create/replace 流程，owner 记录也不会再暴露在普通记忆编辑控件中。

## v6.901.5 — 2026-09-01

### 新增

- **Next 现在既管理 Idea，也管理 Task。** 每个 Task 都是 `inbox/tasks` 下独立的 Markdown 文件，可以创建后直接标记为当前行动；任务内容与由人控制的生命周期台账彼此分离。Vault 指令会要求 agent 只添加由已配置 Vault owner 本人承担的明确工作，并记录项目、来源和去重信息，不能替用户承诺或完成任务。
- **Idea 与 Task 都具备真正可执行的行动信息。** 每张卡片均可显示和编辑优先级、截止日期与 GTD 情境，默认值在 Next 自己的设置页管理；「进行中」泳道会按完整数量持续显示可配置的 WIP 警告，不会被当前搜索或项目筛选掩盖。
- **Vault 可以与 agent 共享稳定的用户模型和长期记忆。** 新 Vault 模板包含负责 owner 身份与稳定画像的 `USER.md`，以及保存有来源的跨会话事实与决定的 `MEMORY.md`。新的 Memory 插件可以列出当前条目、审阅精确的前后增量，并展示确定性的整理建议。
- **Agent 可以通过命令行提出受控记忆变更。** `notemd memory` 支持查看、建议、创建/替换/合并/废止/优先级提案、完整性检查与迁移。批准或拒绝会绑定提案的精确 SHA-256 和显式的人类 actor；只有提案绝不会自动成为已确认记忆。

### 变更

- **Next 设置现在只在 Next 窗口内维护。** WIP 上限和新卡片默认值不再占用 note.md 系统设置的私人标签，既有保存值会继续沿用。
- **受控的 `USER.md` 与 `MEMORY.md` 现在是只读投影。** source/rich 编辑、手动与自动保存、粘贴/拖放、覆盖和历史恢复都会拒绝直接修改。人工批准的 revision 以原子方式写入，并保留不可变候选与决定记录；漂移、过期 revision、提案字节变化或重复决定都会失败关闭。

## v6.901.4 — 2026-09-01

### 变更

- **插件市场现在明确区分认知工作与系统能力。** 「记录、阅读、灵感、推进、回顾、创作」描述用户借助插件完成的事情；「导入与导出、体验增强」则标记为系统功能。Roam Import 与 PDF Export 归入「导入与导出」，Power Mode 独立归入「体验增强」；App、原生插件菜单和公开市场使用相同顺序与名称。
- **一次更新全部有变更的插件。** 有更新时，插件市场会醒目显示「全部更新」按钮；一次确认即可授权整批更新，每个安装包仍会在安装前完成安全校验，单个失败也不会阻断其余插件继续更新。
- **AI 辅助插件更容易被发现，同时 AI 不会变成包罗万象的一级分类。** 页面新增紧凑的「与 AI 一起完成」入口，分别突出 AI 阅读、启发、推理与执行；对应卡片显示具体角色徽标，有更新时仍以更醒目的更新状态为最高视觉优先级。

### 修复

- **已安装插件会立即迁入新分类，离线时也一样。** 正式插件按稳定 ID 在本机迁移旧 manifest 与缓存分类，联网后市场索引继续提供同一套分类。

## v6.901.3 — 2026-09-01

### 修复

- **同一问题在文档里出现多次时，Sidecar 答复现在会与各自批注正确配对。** 即使两个相同问题位于同一段落，或前一个问题仍未回答，每处也只显示自己的答复卡片；采纳其中一个答复或重新生成 Sidecar 时，也只会更新对应的那一处，不再因问题文本相同而串答。

## v6.901.2 — 2026-09-01

### 修复

- **从伴生手记启动 Agent 时，现在会先保存手记。** 未落盘的大纲编辑会立即写入，包括尚未建文件的 sidecar note；只有确认实际保存路径后才会启动 Agent。保存冲突或失败会阻止执行，不再误报“文件位于 vault 外”。

## v6.901.1 — 2026-09-01

### 新增

- **插件托盘快捷键现在会在安装后立即生效。** 启用、停用或移除插件时，系统级快捷键会与托盘菜单同步更新，不再需要重启 note.md。

### 变更

- **本地化插件的托盘顺序现在保持稳定。** 显示名称仍跟随当前界面语言，但切换语言不会再改变入口位置；Next / 下一步会在所有支持语言中紧随 Idea Spark / 奇思妙想。

## v6.831.2 — 2026-08-31

### 变更

- **插件市场现在能在一屏内展示更多插件。** 页头、分类区块、网格间距和卡片都更紧凑，同时保留清楚的分类边界；有可用更新的插件会以橙色卡片高亮、目标版本标记和同色主按钮醒目提醒。

### 修复

- **插件名称与简介现在会在整个市场中跟随界面语言。** 非西文的本地化产品名会在括号中保留英文名，西文名称则保持简洁；已安装插件也会把这些文案写入首屏缓存，在首次刷新后的离线启动中继续正确显示。

## v6.831.1 — 2026-08-31

### 变更

- **插件市场从两段密集列表改成了留白清晰的分类目录。** 每个能力分类只占一个大区块，已安装与可安装插件统一放在其中；响应式卡片会清楚呈现简介、版本、启用状态、可用更新和操作入口。
- **已安装插件会在联网目录加载完成前先出现。** 市场先恢复一份精简的本地快照，再用这台设备的实际安装状态校准；注册表返回后才补齐完整目录和更新状态。断网时，已安装插件也不会再从页面消失。

## v6.829.2 — 2026-08-29

### 变更

- **「复制上下文」现在只保留 agent 所需的两个文件。** 复制内容仅说明源文档的用途与完整路径、手记文件的用途与完整路径，不再附加开场白或操作指令。

## v6.829.1 — 2026-08-29

### 新增

- **Sidecar Notes 现在可以复制一段供其他 agent harness 直接使用的上下文提示。** Agent 区域新增「复制上下文」按钮，内容包含当前文档完整路径、对应高亮手记经过解析的 sidecar note 路径，以及先加载两份文件的清晰说明；复制成功会在原位短暂提示，按钮和复制内容均跟随当前界面语言。

### 修复

- **位置记录现在可以正常请求 macOS 定位权限，并清楚说明权限失败原因。** 签名构建已包含系统授权弹窗所需的 Location entitlement；若权限被关闭，警告会直接给出「系统设置 → 隐私与安全性 → 定位服务」路径，不再只显示 `kCLErrorDomain code=1` 原始错误。

## v6.828.6 — 2026-08-28

### 新增

- **可用的 Mermaid 图形家族与样式控制更多了。** 内置引擎新增 Event Modeling、`cynefin-beta`、泳道图和 railroad beta 系列，同时支持环形饼图、更丰富的 XY 图标签/图例、可折叠 flowchart 子图、更多 flowchart 形状以及 ER 子图。

### 变更

- **Mermaid 从 11.14.0 安全升级到 11.17.2，note.md 的所有渲染入口统一使用这一精确版本。** 富文本编辑、预览、分享、打印与 PDF 导出不会再解析到不同的 Mermaid 版本；图形代码仍然按需加载，因此不会增大 note.md 的初始应用 chunk。
- **新 Vault 会明确告诉 AI agent 可以生成哪些 Mermaid。** 默认 `AGENTS.md` 现在会列出内置 11.17.2 的图形家族、Unicode 与样式能力、beta 边界和可靠生成规则；已有 Vault 的用户指令不会被改写。
- **若干上游渲染器默认值会有意产生更整洁的输出。** classDiagram 默认使用 Mermaid 统一渲染器；C4 改用统一的纯 SVG 形状并支持长标签换行；带点号的 class namespace 按层级组织；tree-view 图标默认隐藏。既有语法仍然有效，但这些图形的间距、形状或标记可能与 11.14 略有差异。
- **Mermaid 自定义 CSS 的解析更严格。** 无效或括号不平衡的 `classDef`/主题规则及不安全的 at-rule 会被丢弃，不再泄漏到页面。状态图注释必须使用 `%%`；单个 `%` 现在会被当作普通图形文本。

### 修复

- **`quadrantChart` 现在可以直接使用未加引号的中日韩文字、重音字符和 emoji 标签。** 坐标轴名称、象限标题与数据点名称不再触发 11.14 的 lexer 错误；`architecture-beta` 的标题与标签也能直接使用 Unicode 以及 `/`、`-` 等标点。
- **同一区间内的 Mermaid 连线、标记与标签回归一并修复。** 包括恢复 flowchart/block 的 `edgePaths`、flowchart 与状态图只绘制一条自环路径、正确缩放 classDiagram 关系标记、深色主题下的 ELK 箭头、classDef 文字颜色、宽 block 标签、C4 长标签换行、sequence 的 `alt`/`else` 与 `rect` 背景、RTL 消息对齐、Gantt 标记位置、雷达图标签、Venn 并集、XY 图标签以及大型 tree-view 标签。
- **Mermaid 11.17 在 note.md 仍支持的旧版 macOS WebView 上也能工作。** 当 WebKit 已暴露 stylesheet、却还不能直接构造 `CSSStyleSheet` 时，兼容路径会提供新版安全样式净化器需要的 CSSOM 对象。

### 安全

- **Mermaid 及其 SVG/CSS 净化依赖已包含当前上游安全修复。** 本次升级清除了旧图形依赖锁定版本涉及的 Mermaid、DOMPurify 与 UUID 告警，包括畸形图形/CSS 注入及拒绝服务问题。

## v6.828.5 — 2026-08-28

### 修复

- **奇思妙想和溯源的富文本编辑器现在会立即跟随主题字体。** 已打开的插件窗口不再因设置落盘竞态而短暂保留旧主题；Effie 内置的霞鹜文楷 Webfont 也通过了插件沙箱中精确限定的字体策略，不会再静默回退为系统字体。修复随 note.md 主应用提供，无需单独更新插件。

- **Idea Spark 恢复识别既有的 `*-idea.md` 文件名约定。** 收件箱现在只列出严格以该小写后缀结尾的普通文件；新建和重命名也使用同一后缀，proof 文档、其他 Markdown、目录及 `.idea.md` 文件均不会进入列表。

## v6.828.4 — 2026-08-28

### 变更

- **Position Log 已统一更名为 Location Log / 位置记录。** 插件名称和「记录」分类下的菜单入口现在使用同一个简洁名称；内部插件 ID 仍为 `notemd.pos-log`，现有安装会原位升级。

### 修复

- **原生「插件」菜单现在会完整显示 `Import & Export` 中的 `&`。** 构建分类子菜单前会转义底层菜单库的助记键标记。

## v6.828.3 — 2026-08-28

### 变更

- **插件分类改为简洁、任务导向的单归属命名。** 顶部「插件」菜单和插件市场现在统一使用：智能体、记录、阅读、思考、导入与导出、编辑。升级期间仍能识别上一版分类 key，未知第三方值继续安全落入「其他」。

## v6.828.2 — 2026-08-28

### 变更

- **插件不再挤成一条长菜单，而是按能力分组。** 顶部「插件」菜单、App 内插件市场和官网市场现在共用五组：智能体、采集与导入、思考与复盘、发布与导出、编辑增强。Claude、Codex、DeepSeek 统一收进「智能体」；未知第三方分组仍会显示在「其他」，不会丢失入口。

### 修复

- **奇思妙想现在只列出明确命名为 `*.idea.md` 的文件。** idea 目录里的普通 Markdown、论证文档、目录和大小写不匹配的后缀不会再被算成想法；新建和重命名会保持 `.idea.md` 后缀，孤立的论证文档也不会再让一个新想法误显示为已经论证。

## v6.828.1 — 2026-08-28

### 修复

- **Agent 并发设置已回到各 Agent 插件自己的设置页。** Claude Agent、Codex Agent、DeepSeek Agent 窗口左栏底部各有独立的「设置」入口，页面保留原来的 1–5「AI 阅读最大并行数」，此前选过的值也会继续使用。note.md 全局设置里的三个插件标签已移除；每个插件只能读写自己的设置作用域。

## v6.827.2 — 2026-08-27

### 新增

- **AI 读书现在可由多个 agent 并行处理，同时严格遵守各 provider 的额度。** Claude Agent、Codex Agent、DeepSeek Agent 的设置页各新增「AI 阅读最大并行数」，范围 1–5、默认 1。电子书导入会为每个 provider 维护独立 FIFO：Claude 与 DeepSeek 可以同时读，同属 Claude 的额外任务则只在 Claude 自己的额度后排队。运行中修改设置也会生效；降容不会打断正在读的书，同一本书仍不会被重复派发。
- **Codex 现在是 note.md 的一等智能体。** 安装 Codex Agent 插件后，它会直接驱动你本机已经登录的 Codex CLI：智能体窗口可实时显示进度、保留运行历史、取消任务，也可通过 `notemd codex-agent` 使用同一套流程。内置任务会共用 note.md 的任务目录，但不会覆盖其他 provider 的指令；每次运行则使用 Codex 原生的只读、工作区可写或完全访问沙箱。
- **导入电子书:书库全在眼前,任何一本都能重新让 AI 读一遍。** 以前这个窗口只认得你这一次拖进来的文件 —— 上个月导入的书够不着,想重读只能再导一次。现在导入队列下面多了「书库」,列出 vault 里已有的每一本书,可按书名搜索,标着导入月份和最近一份 AI 摘要的日期。每一行都能打开 `book.md`、打开摘要,或者挑一个 agent 重新读一遍。重读覆盖的是**当天**那份摘要,以前的原样留着,历史不丢。同一本书从队列和书库各点一次,也不会跑两遍了 —— 后点的那次会跟上前一次,而不是和它抢着写同一个文件。
- **AI 阅读的提示词,是一个你可以改的文件。** 设置里新增一行 `.notemd/agent-tasks/ai-read-ebook/CLAUDE.md` —— agent 读书时遵循的那份说明。点开就在主窗口里编辑,和别的笔记没两样。你想让摘要多说什么、少说什么,改完之后每一次 AI 阅读都按你的版本来。(这个文件在 Claude Agent 第一次运行后才出现;没有时那一行会告诉你。)
- **电子书加入时间现在会明确落盘。** 每本新导入书籍的目录都会生成 `meta.yml`，以 RFC 3339 UTC 标准的 `added_at`（`YYYY-MM-DDTHH:MM:SSZ`）记录加入时刻，不再需要从月份目录或可能变化的文件时间反推，也没有时区歧义。书库会按这个时间从新到旧排列。

## v6.827.1 — 2026-08-27

### 修复

- **DeepSeek Agent 现在可以正确交付任务工作区之外的电子书摘要。** AI 阅读会在本次运行的独立 workspace 内准备原书和目标摘要，再只把声明的结果回写 Vault；不会再在最后写文件时撞上沙箱拒绝，并发运行也不会把别的任务产物认成自己的。
- **Codex Agent 现在跟随 Vault 的真实 Codex 模型，已完成的长任务也不会再误报失败。** Codex 会从 Vault 根目录启动，解析与用户在该目录运行 CLI 时相同的有效模型，并把真实模型写入 AI 署名，不再使用 `codex/default` 占位符。收到成功的 `turn.completed` 后，即使 CLI 交付文件后的收尾超过三秒，也会保持成功状态。
- **插件窗口也补上了输入法那个修复。** 奇思妙想、溯源、Power Mode 写字用的就是主窗口那套富文本编辑器,v6.824.3 修掉的毛病它们一样有:把输入法候选退到空会多删前面一个字,确认候选的那一下回车也可能被处理两遍。这个修复由应用下发,不用更新插件。

## v6.824.3 — 2026-08-24

### 修复

- **在输入法框里一个一个删,删到最后一个时不再把前面那个字一起删掉。** v6.824.2 修的是变换过程中的退格,漏掉了最特殊的那一下 —— 它同时做两件事:删掉最后一个预编辑字符、结束这段变换。有些 webview 会**先**宣布变换结束、**再**把这一下按键派发出来,于是它到手时已经不像输入法的键了,编辑器把它当成了一次普通编辑。现在这一段变换连同收尾的那一下按键一起算作输入法的,把候选退到空不会再动前面的正文。大纲、源码模式、富文本、批注弹窗、回溯块编辑用的是同一条规则,确认候选的那一下回车也不会再被读成「提交这一行」。富文本那边光「不管」还不够:编辑器内核其实认得这一击,但它只是不处理,浏览器自己的删除照样跑 —— 现在直接把这一击取消掉。

## v6.824.2 — 2026-08-24

### 修复

- **输入法候选窗口开着时按退格,不再多删一个字。** 在大纲里打中文/日文/韩文,想退一格改预编辑串,结果是输入法删掉一个字符、我们又把这一行并进了上一行 —— 同一下按键被处理了两遍。现在变换中的按键全应用一律归输入法:大纲的退格/回车/上下键行命令、源码编辑器的成对符号自动补全、富文本的斜杠菜单、批注弹窗的回车保存、回溯块编辑的回车提交,都等候选窗口关掉再说。文字一旦确认,行为和以前一模一样。
- **rich 模式下 Cmd+A 重新能全选整篇。** 之前只选中一部分 —— 要么只有开头那个折叠的 frontmatter 块,要么光标在代码块里时只选中那一个块 —— 而同一个文件上右键 ▸ 全选却是好的。原因是快捷键是唯一没走编辑器真正那套全选的路径:macOS 把 Cmd+A 当成「编辑」菜单的快捷键先吃掉了,漏过去的又落在底层编辑器自带的粗糙 `Mod-a` 上。现在键盘、右键、菜单三个入口用的是同一个选区。有一处需要知道的连带变化:「编辑 ▸ 全选」旁边不再显示 Cmd+A 字样,同时 Cmd+A 也不会再被从搜索框等输入框里抢走 —— 谁有焦点就全选谁。

## v6.824.1 — 2026-08-24

### 新增

- **`notemd .` 与 `notemd xxx.md`:命令行直接把东西开进应用。** 不用记子命令,给 `notemd` 一个路径就行 —— 文件开成标签页,目录成为文件夹视图的根。路径按 shell 的当前目录解析,相对路径照样能用;应用已经开着就交给那个窗口,不会再开一个,命令本身立刻返回,不等窗口。文件不存在会明说(`cannot open 'x.md': No such file or directory`),而不是「unknown command」;同名时命令仍然优先 —— 想开那个文件请写 `./search`。详见 `notemd help open`。

## v6.820.1 — 2026-08-20

### 修复

- **`notemd reading-insights` 的命令行报告不再永远是空的。** 它一直在 vault 里找 `.mdeditor/analytics/`,而应用早就写到 `.notemd/analytics/` 了 —— 目录不存在,脚本就安静地报了个空。产品改名时漏掉的一处。

### 新增

- **溯源有了收件箱和设置。**(插件市场:溯源 1.1.0。)报告默认落在 `inbox/traces/`(可在窗口设置里改),文件名形如 `<日期>-<时刻>-source-trace.md`,材料全文放在报告旁的同名文件夹里。新的收件箱面板 —— 和奇思妙想同一个样式 —— 倒序列出历史报告;点一下在主编辑器里阅读,右键可打开或删除(连材料一起)。溯源运行期间,底栏实时显示进度,报告一落地收件箱自己刷新。委托提示词可从设置里点开编辑;任务模板会自动迁移一次,这回不用手动删了。
- **溯源任务再也不会凭空丢失。**(插件市场:溯源 1.2.0。)点下委托的那一刻,你的原文先落盘 —— 存在报告旁的 `…-source-trace/00-request.md` —— 然后才轮到 agent;运行本身也有登记,窗口关掉或刷新后重开会自动接上:还在跑的显示 ⧖,跑完的显示报告,什么都没产出的显示 ✗,而不是消失。失败行右键可以把委托稿载回编辑器再来一次。委托稿保存失败时干脆拒绝委托,而不是没有保险就悄悄开跑。
- **MCP server**:note.md 现在可以把 vault 的检索能力供给 agent。在 Claude Code /
  Cowork / Codex 里注册 `{"command": "notemd", "args": ["mcp"]}` 即可,agent 便能用上
  中文分词、相关性排序与 origin 加权,而不是退回 grep。只读,不开网络端口。

## v6.818.8 — 2026-08-18

### 变更

- **溯源独立成了自己的插件。** 从插件市场装上「溯源」(Trace Source),右键菜单里的「溯源」项才会出现;没装就不出现 —— 而不是点了却打不开一个不存在的窗口。选中一段话右键「溯源」,会打开一个专属委托窗,引文已经填好;补上范围说明(「只查 YouTube 和 arxiv」)交给 agent,报告落在 `traces/`,写好后有通知。插件菜单里也有一项,打开空白委托窗,用来处理从别处复制来的文字。
- **`/溯源` 指令没有了,委托文本也说你的语言。** 专属窗口本身就是溯源模式,不再需要输入指令词;英文/日文界面里也不会再冒出中文协议字段 —— 机器读的那几行改成了语言中立的 `Source-Doc:` / `Output:`。奇思妙想回归纯粹:只管记想法、论证想法,它此前为溯源背着的指令层已整体移除。(这次发布之前用过溯源的话,请删一次 `.notemd/agent-tasks/trace-source/`,好让新模板重新播种。)

## v6.818.7 — 2026-08-18

### 修复

- **不再报「DeepSeek Harness 装了,但起不来:env: node: No such file or directory」。** 两个 agent 都是通过一个 Node 脚本启动的,而从程序坞启动的应用继承到的 `PATH` 比终端里窄得多 —— 里面没有 `node`。真正运行时一直会补上这个差,但「报告你装了哪套环境」那步没有补,于是装得好好的环境被报成坏的,而报成坏的又会把运行按钮禁掉。你这边不需要做任何事。

## v6.818.6 — 2026-08-18

### 变更

- **DeepSeek 智能体改用你自己安装的 DeepSeek Harness。** 它会在你机器上找 `dsh`,第一次运行时把 ACP 桥装进一个专属的 `notemd` profile(`dsh plugin --profile notemd add @deepseek-ai/dsh-acp`),然后经它启动。harness 给你的一切 —— 工具、技能、子 agent、网页检索 —— 都跟着来,因为组合由 profile 负责,而不是一份只能挂上子集的手写配置。装它:`npm i -g @deepseek-ai/dsh`,剩下的插件自己搞定。
- **vault 里那份 harness 配置从「整份组合」变成「覆盖层」。** `<vault>/.notemd/dsh/cordis.patch.yml` 只写 note.md 要改的部分 —— 哪里可写、用哪个模型、以及关掉一个会冲垮协议流的东西 —— 叠在你的 profile 之上。它仍然是 vault 里的纯文本,仍然归你改。你没动过的旧 `cordis.yml` 会在首次运行时被清掉。

### 修复

- **不再报 "Cannot find package 'tsx'"。** 插件此前会退而去驱动一份 DeepSeek Harness 的**源码 checkout**,那需要钉死版本的包管理器、一个开发依赖、以及九百多个包的安装 —— 没人会有这些,而它们失败的方式又和你的 vault 毫无关系。这条路已删除。安装提示现在只指那条真正可用的。

## v6.818.5 — 2026-08-18

### 修复

- **DeepSeek Agent 失败时不再「一片空白」。** 真实事故:harness checkout 依赖残缺时,pnpm 会在起服务前静默补装数分钟、网络一抖就失败——而这一切只打在 stdout,窗口里连一行日志都看不到。两处都修了:运行期不再自动补装(依赖坏了 2 秒内快速失败,并给出「去 checkout 里跑 pnpm install」能看懂的报错);运行失败时,启动器/安装器打在 stdout 的最后输出会保留进运行记录,窗口里能看到失败原因,不再双手一摊。

### 新增

- **奇思妙想:交给 agent 的那段提示词,现在可以自己改。** 设置里多了「委托提示词」一栏,列出「论证想法」以及你手上每个 `/指令` 的提示词,点一下就用标准 Markdown 编辑器打开。提示词本来就是 vault 里的一个普通文件(`.notemd/agent-tasks/<任务>/CLAUDE.md`)—— 这次只是给它开了个门。改完下次委托即生效,插件永远不会覆盖你写下的内容。

### 修复

- **手记里改选另一个 agent 现在真的生效了。** 选择存在一个界面并不监听的值里,于是勾选标记和「由 … 执行」仍显示原来那个 —— 看起来就像选 DeepSeek 失败了。现在选择立即生效,并像其他入口一样重启后仍记得;运行中的任务也会继续通过启动它的那个 agent 汇报状态,不会因为之后改了选择而报「找不到运行」。

## v6.818.4 — 2026-08-18

### 修复

- **选择 agent 的下拉菜单不再被窗口裁掉。** 原来它定位在所在面板内部,于是外面任何一层滚动容器 —— 手记面板、电子书队列 —— 都会在它还没碰到窗口边缘时就先把它切掉。现在它浮在最上层,并自己决定方向:默认向下,按钮靠近底部时向上,窄面板里则横向让位而不是滑出边缘。滚动时它跟着按钮走,不会飘到别处。

### 新增

- **溯源:选中一段话,查出它的原始出处。** 编辑器里选中文本 → 右键「溯源」,或在奇思妙想里输入 `/溯源`,委托 agent 去 YouTube、论文库、欧美技术博客定位原文(检索范围可在委托文本里指定),下载字幕或正文,生成一篇带反向链接的摘要落在 vault 的 `traces/` 目录——摘要里能一键跳回你提问的文档,也能顺着相对链接继续读抓下来的全文材料。完成后通知直达,点开即读。YouTube 字幕依赖本机 `yt-dlp`(没装则降级为链接+描述,并在摘要里如实声明)。
- **奇思妙想支持 `/指令`。** 任何在 `.notemd/agent-tasks/` 里声明了 `directive` 的 agent 任务模板,都会自动成为输入面的 `/xxx` 指令——自己写一个模板就能造出自己的指令,溯源只是第一个。

## v6.818.3 — 2026-08-18

### 变更

- **选哪个 agent 执行,全应用统一成一种交互。** 凡是把活儿交给 agent 的按钮,旁边都带上同一个控件 —— `[ 回答 ] 由 Claude 执行 ▾` —— 手记面板、奇思妙想、电子书队列的「AI 先读」都一样。下拉里列出每个已装 agent 的 harness 版本号与将要使用的模型,并勾出当前选用的那个,点之前就知道这次要花的是谁的 token。harness 起不来的 agent 会连原因一起显示,而不是装作可用。
- **每个入口各自记住自己的选择。** 用一个 agent 论证想法、同时让另一个读书,是很正常的需求,所以选择按入口记,而不是一个全局设置。已经排进队列的电子书仍然用入队时选定的那个 agent 跑 —— 等待期间改了选择框也不会串。
- **agent 窗口会写明自己是哪套环境。** Claude 智能体和 DeepSeek 智能体在运行按钮旁显示 `由 Claude 执行 · 2.1.233` —— 只陈述,不给下拉:那个窗口本身就是那个 agent,在里面切换只能把活儿支到别处去。

## v6.818.2 — 2026-08-18

### 修复

- **两个 agent 窗口不再一直停在「正在检查运行环境…」。** 横幅是通过窗口自己的通道去问插件的,而插件只在菜单/中转那条通道上应答,于是窗口的提问永远没有回音 —— 加上「查询失败」和「还在查询」渲染成了同一个样子,转圈就再也停不下来。两处都修了:窗口通道上也应答,查询失败会明说。
- **装了的 harness 不再被报成「未安装」。** 装了但起不来(版本钉死、依赖缺失)现在显示「装了,但起不来」,并给出原因和找到它的位置,而不是让你去装一个你已经有的东西。
- **DeepSeek Harness 从本地 checkout 又能用了。** harness 仓库把 pnpm 版本钉死,corepack 遇到版本不符会直接拒绝运行而不是切换 —— 于是只要你的 pnpm 不是那个版本,这个 agent 就报不可用。现在启动器把这条钉死降级为警告。版本号也改为直接从 checkout 里读,不再靠启动一个服务端去问 —— 既快又准。
- **安装提示不再推荐一条走不通的路。** 原来写的是 `npm i -g @deepseek-ai/dsh-acp-demo`,但这个包依赖的 `dsh-workspace-context` / `dsh-bash-env` 根本没发布到 npm,装不上、或者装上了也起不来。提示改为指向确实可用的本地 checkout 方案。

## v6.818.1 — 2026-08-18

### 修复

- **手记下面的 Agent 区域回来了。** v6.817.4 改成用一个宿主根本不会发给前端的清单字段来判断「装没装 agent 插件」,于是答案永远是「没有」,整块区域就此消失。现在改用宿主计算并下发的标记来判断。
- **每次运行都会记下是谁执行的。** 两个 agent 插件共用同一个任务目录和同一份运行历史,所以一次 Claude 的运行会原样出现在 DeepSeek 智能体窗口里 —— 这正是 **Claude** 登录过期被读成 DeepSeek 失败的原因。现在每次运行都记录执行者,来自另一个 agent 的历史行会被标注,每个窗口的环境告警也只报告自己那套 harness 的问题。(本次更新之前的运行记录没有执行者信息,不会被任何一方认领。)

### 新增

- **可以选谁来答。** 两个 agent 都装了时,手记下面的 Agent 区域会出现一个选择框,显示各自的 harness、版本与模型 —— 比如「Claude Code · 2.1.233」—— 点下去之前就知道这次要花的是谁的 token。
- **每个 agent 窗口开门见山地报告自己的环境。** 任务列表上方一条横幅:harness 名称、版本号、任务没指定模型时会用的默认模型,以及可执行文件是从哪儿找到的。如果 harness 在但起不来,直接说明原因,而不是自称就绪。
- **登录过期就说登录过期。** 最近一次运行如果是因为环境问题失败的(凭据过期、缺 API key、被限流),窗口会明说,而不是让它看起来像任务本身有毛病。

## v6.817.4 — 2026-08-17

### 新增

- **第二个 agent,以及在两者之间选择的方式。** 新插件 **DeepSeek 智能体**(插件市场)经 Agent Client Protocol 把 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness) 接进 vault,与既有的 Claude 智能体并列。两者读同一批 `.notemd/agent-tasks/` 任务模板、写同一种运行记录、回答手记里同样的问题 —— 任务是一份活儿的描述,不是对某个模型的绑定。谁来服务 agent 插槽现在是一项设置(`<vault>/.notemd/settings.json` 里的 `agentDefaultProvider`);只装了 Claude 智能体时行为与今天完全一致。
- **任务权限是一份你能读的文件。** DeepSeek 智能体的任务带一份 `policy.json`,写明这次运行被关在哪一档沙箱里(`read-only` / `workspace-write` / `danger-full-access`),以及 agent 中途要权限时怎么答。档位由 harness 自己的沙箱强制执行,不靠提示词自觉,任务列表在你按「运行」之前就把它显示出来。`policy.json` 解析不了会直接停住运行,而不是退回默认值。
- **harness 的组合配置住在你的 vault 里。** `<vault>/.notemd/dsh/cordis.yml` —— 纯文本、进 git、可以直接手改;想用自己那份,用 `dsh_config` 设置指过去即可。

### 说明

- DeepSeek 智能体目前是**实验性**的:它依赖 DeepSeek Harness 的开发者预览版,上游明示会有破坏性变更。使用前需要 `npm i -g @deepseek-ai/dsh-acp-demo`(或一份 `deepseek-harness` checkout),以及 `DEEPSEEK_API_KEY`。该协议只传输已提交的助手文本 —— 没有实时工具活动,也没有会话续传 —— 所以 DeepSeek 运行的日志比 Claude 运行的安静。这是协议如此,不是功能缺失。

## v6.817.3 — 2026-08-17

### 修复

- **手记再也不会漏进源文件夹。** 通过「同步到 Vault」菜单同步的外部文件,在原文里做批注(比如提问)再保存时,曾会在 vault 外的原始文件旁悄悄生成第二份 `.note.md` —— 一个 agent 和后续编辑都不会再更新的过期孤儿,而真正的手记住在 vault 副本旁。造成这一切的遗留双向镜像机制已整体移除:手记只住 vault,任何同步路径都不再写源目录。源文件旁**已有**的旧笔记首次同步时仍会被收养进 vault(仅复制,源文件原样保留)。文件夹视图的「有笔记」徽标也改为只在同步文件确有手记时点亮,不再对所有分享/同步过的文件都亮。
- **决策日志不再因两条决策共用同一个 id 而永远停在「加载中…」。**(插件市场:决策日志 1.1.3。)agent 写的日文件曾把同一个 id 分配给两条不同的决策;两条都归档后,看板渲染在重复 key 上崩溃,窗口永远停在加载页。现在即使 id 重复,看板也会把每条都渲染出来。

## v6.817.2 — 2026-08-17

### 修复

- **注意力加权检索此前对所有用户完全没生效。** 摄取读日文件时假设的结构比实际多了一层嵌套,导致每份文件解析失败后被静默跳过 —— 日志显示 `attention ingest: 0 files`、设置页显示 `0 / 9581`,而排序里一点注意力都没算进去。现已按盘上真实结构解析;实测你的 vault:60 份日文件、537 个文件带注意力数据。

## v6.817.1 — 2026-08-17

> **升级后首次打开 vault 时,搜索索引会全量重建一次**(约十秒,期间搜索面板显示"正在构建")。索引是可弃的派生数据,重建不会动 vault 里的任何文件。

### 新增

- **检索开始计入你的阅读时长。** 你在一篇文档上花过的时间(阅读 + 编辑,编辑按 1.5 倍计)会让它在搜索结果里更靠前,并按 30 天半衰期衰减 —— 上个月读透的东西让位给这周在看的。数据来自本就在采集的「阅读洞察」,不新增任何采集。从没打开过的文档**不会被惩罚**,只是不加分:AI 刚生成、你还没读的文档,正是你要搜出来的。
- **你读得多的长文不再被埋掉。** 一篇你读了三小时、但查询词只出现一次的长文,以前在打分之前就被相关度截断砍掉了;现在它有一个进入排序的保底名额。
- **设置 ▸ 搜索**新增「有注意力数据的文件」一行,显示这项数据覆盖了多少文件。
- **`notemd search --json` 新增 `attention_minutes` 字段** —— 已衰减到今天的注意力分钟数,让 agent 能解释排序为什么是这个顺序。纯文本输出 `path:line:text` 一个字没改。
- **新增 `notemd doctor` 自检命令** —— 一条命令检查环境、配置与 vault、搜索索引、插件、网络端点五组状态,分别报出问题原因,而不是笼统说"不工作"。

### 变更

- 搜索结果顺序会变。这是上面那条的直接结果:你投入过时间的文档整体上移。
- 注意力加权的强度可调:`.notemd/settings.json` 里的 `searchWeights.attention`(默认 `0.4`,设为 `0` 即完全关闭)。改完下一次搜索即生效,不用重开 vault。

### 修复

- vault 外的源文件被镜像进 vault 后,你在源文件上花的阅读时间现在会正确归到 vault 里的镜像副本上,不再丢失。
