# Timeline 显示插件

## 目标与边界

新增 `notemd.timeline`，将 frontmatter `type: timeline`（忽略大小写）的 Markdown 在富文本模式中显示为竖向日程。文档仍是 Markdown，源码切换、保存和外部变更使用宿主现有链路。插件不改写文档；加载失败、无响应或任一时间项解析失败时回到 Markdown 编辑器。用户主动编辑后保持编辑状态，点击返回视图或切换模式再尝试显示。

## 样本与解析契约

只读检查 diary 下全部 468 篇 `YYYY-MM-DD.timeline.md`，涵盖 2025-05-29 至 2026-09-08：一级活动 6633 条，子议题 1655 条，空日 7 篇，零时长 1671 条，434 篇存在交叠。

支持独立 YAML 分隔行、`Timeline` 类型、日期标题/摘要/说明，以及 `- HH:mm:ss–HH:mm:ss — 动作：描述 [来源](relative.md#line10)`。兼容分钟精度和常见连字符，保留秒精度、零时长及嵌套议题。时间须有效且结束不早于开始；允许 24:00 作为终点，倒序跨夜记录交给 Markdown 编辑器。任一不支持的列表或损坏条目整体回退，不能静默漏项。正文说明保留可见。来源只打开当前 Vault 内相对文件，现有 host.editor.open 只定位文件，不承诺行锚点跳转。

日程按开始时间排序；左侧时间刻度，时间块按时间定位，短活动设最小可点击高度，重叠活动分列。子议题在父活动详情中展示并继承分类。时段交叠不累加为投入时长。

## 分类

固定大类：工作（浅蓝）、兴趣（浅绿）、生活（浅粉）、休闲（浅灰）、其他（白）。设置包含有序规则：名称、关键词数组、所属大类。**只对活动类别文字做关键词包含匹配**，忽略大小写，不匹配正文，不做语义推断；规则从上到下首条命中，未命中为其他，空关键词无效。规则名称仅用于管理。

默认按样本常见动作分组：审阅/审查/评审，开发/修复/测试/发布，沟通/会议/讨论，设计/组织/规划属于工作；阅读/学习/实验/实践等属于兴趣；家庭/出行/采购等属于生活；聚餐/散步/娱乐等属于休闲。用户可新增、修改、删除和调整规则顺序。

设置在插件内部页面编辑，`host.settings.get/set` 存在插件域 `classification` 键；保存成功后立即生效，取消不改变现行规则，读取/保存失败可见且保留草稿供重试。manifest 不贡献系统设置表单。

## 宿主契约

复用 `custom_editors`，增加 `markdown_types?: string[]`，扩展名注册继续禁止劫持 `.md`。type 显示插件只用于普通 Markdown，源码模式和受管 USER/MEMORY 不受影响。独立 iframe 与宿主交换 `custom_editor.open`（含 `requestId`）、`custom_editor.ready` 和 `custom_editor.fallback`；宿主同时验证 origin、source 与当前 requestId，显示通道不接受文档 `change`。加载/内容更新期间不显示上一份内容；8 秒无响应回退。旧 custom editor 协议保持兼容。显示 iframe 开启 `allow-forms` 以允许设置表单的提交事件；表单 `preventDefault` 后仅调用 settings RPC，插件原有 CSP `form-action 'none'` 保持禁止页面表单导航。

当前公开宿主尚无此 type 入口，插件 minimum host 预留 `>=6.909.0`，须随包含该入口的宿主发布后才能安装使用。本任务只实现、构建、验证及本地提交，不上架发布。

## 验证

- 全部真实样本解析条数、父子层级与动作提取完整。
- 解析异常、伪造/过期消息、超时、外部内容更新与手动 Markdown 编辑回退。
- 关键词包含匹配、规则优先级、空词拒绝、取消/保存失败/重试/重开持久化。
- 真实浏览器检查浅色/深色/窄窗、重叠与零时长块、详情与来源；测试以合成数据隔离真实 Vault。
- 插件和宿主相关测试、类型检查及生产构建；保留现有 weekly-review 工作区改动。

## 验收结果（2026-09-09）

- 真实 468/468 篇均被宿主正确匹配并被插件完整解析，6633 个一级动作与原文逐一一致，1655 个子议题、7 个空日与1671 个零时长条目保留。
- 插件 68 项、宿主与打包脚本 194 项相关测试通过。插件类型检查零错误零警告；宿主零错误、37 个既有警告；两者生产构建及 Editor Kit 构建检查通过。
- Chromium 140 的 11 组跨 origin 验收通过：浅色/深色/390px 窗口、详情/子议题/来源、分类失败草稿与重试持久化、手动编辑往返、外部重载与解析回退、伪造消息隔离、源码/无插件/不支持类型、读取失败、损坏配置修复、无响应加载超时。
- 浏览器使用真实 Svelte 组件及内存设置/导航 RPC，未安装到用户 App、未验证原生 WKWebView 或发布宿主/插件；真实 diary 只读。浏览器发现并修复了 iframe 的表单提交限制，并验证 `allow-forms` 与原有 `form-action 'none'` 共存。
